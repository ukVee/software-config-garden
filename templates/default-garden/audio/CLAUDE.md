# audio/

The audio stack: the sound server (PipeWire/PulseAudio/etc.), routing, devices, per-app quirks. Commentary only.

## How to behave here

- Document the sound server in use, default sinks/sources, and any routing or latency tweaks.

## Cross-refs

- `services/` — the audio daemon as a service.
- `hardware/` — the physical audio devices.
