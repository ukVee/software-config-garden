# software-config_garden

Implementation repo for **soft-fig** — the program that combines machine memory (the CLAUDE.md garden schema), templating, symlink deployment, integrated VCS, and the Keeper client into one tool. Distributed via AUR.

## Status

**M1a + M1b + M1c + M1d + M2a + M2b + M2c done (2026-05-05 → 2026-05-16); M-onboard done (2026-05-26); M3a done (2026-05-30); M3b done (2026-05-31); M3b vault-surface follow-up done (2026-06-01); M4a (deploy spine) done (2026-06-02).** This garden is self-hosting under signed VCS history with two encryption layers at three granularities: Layer A whole-garden ciphertext, Layer B per-file selective secrets, and M2c per-region inline `<vault id="…">…</vault>` secrets. The daemon mounts a FUSE plaintext-view at the garden root when configured with a relocated `state_root`; sealed paths (matching `.softfig/vault/sealed-paths.toml` globs) are encrypted under per-file HKDF subkeys and projected as `[sealed:<path>]\n` placeholders. Files containing inline `<vault>` tags get per-region HKDF subkeys, base64-inline ciphertext on disk, and `<vault id="…">[encrypted]</vault>` projected through FUSE on read. `softfig reveal <path>` writes plaintext to a 0600 file under `$XDG_RUNTIME_DIR` and commits a `vault_reveal` audit intent; the `--id <id>` repeatable flag reveals just one or more region plaintexts. The same `softfig-keeperd` binary handles M1c-compat, M2a, M2b, and M2c configs from one binary; Layer B is inert until the first `sealed-paths.toml` lands and inline-region encryption is inert until `<vault>` bytes appear in a tracked file.

**M-onboard** adds the first-run front door: `softfig onboard` scaffolds a fresh garden from an embedded default-layout skeleton (`templates/default-garden/`, baked in via `include_dir!`), inits the Vault, and writes a born-in-FUSE genesis commit directly in the `state_root` layout — no plaintext is ever written at the garden root, and the three-phase `migrate` dance is reserved for converting the legacy tablet garden. The scaffold logic lives in the frontend-agnostic `softfig-onboard` crate so a future MCP tool wraps the same `onboard()` entry point. The daemon now resolves its config via `KeeperConfig::discover` (reads `<garden>/.softfig/keeper.toml`), so a born-in-FUSE or migrated garden boots straight into FUSE mode. Install plumbing is a documented runbook (`docs/onboard-laptop.md`) plus `scripts/onboard-device.sh`.

**M3b** adds the first Keeper *human* surface: `softfig-tui`, a ratatui frontend over the daemon (master/detail Browse + History + Action forms + in-app unlock). It introduces two **read-only** IPC verbs — `list_tree` and `read_file` — that serve garden content from the committed tip and apply the same `LayerBHook` redaction the FUSE read path uses, so the TUI receives `[sealed:<path>]` / `[encrypted]` projections, never sealed plaintext. The TUI is a separate binary; the lean-core slice keeps vault reveal/seal surfaces CLI-only for now.

**M4a** opens the **templating + symlinks pillar** (the `bombadil link` replacement) with its static deploy spine: a new frontend-neutral `softfig-deploy` crate (`plan` diff / `apply` I/O) + a thin `softfig deploy [--dry-run] [--force]` CLI. It materializes a declarative `config/deploy.toml` source→target table onto the real filesystem. Because the garden's plaintext is FUSE-only and ephemeral (it vanishes on lock), a direct symlink to a source would dangle — so `apply` materializes each source to a **persistent plaintext deploy-cache** (`~/.local/share/softfig/deployed/`, dir `0700` / files `0600`) and symlinks the target → the cache file (bombadil's `.dots/` model, survives lock); a dot may opt into `method = "copy"` to write the bytes straight to the target with a `# managed by softfig` stamp. Reads + writes are native FS — no new IPC verb; deploy requires the garden unlocked. M4a is **`$HOME` targets + regular-file dots only**; templating (MiniJinja, M4b), Vault secret resolution + posthooks (M4c), and `/etc` deploy are deferred. Sources move into a garden `config/{source,docs}` tree at the future dogfood ceremony.

**M5a (transport + pairing) — COMPLETE (2026-06-09 → 2026-06-10)** opens the **cross-device sync pillar** with its foundation slice: two daemons discover each other, mutually authenticate, establish an end-to-end-encrypted channel (LAN-direct or relayed), and persist each other in a signed network trust ring. **No data crosses yet** (that's M5b+). New frontend-neutral **`softfig-net`** crate (12th member): an application-level **Noise** tunnel (`snow`; `XX` for first pairing, `IK` for reconnect; X25519 + ChaCha20-Poly1305) over plain TCP carrying a **protobuf** (`prost`) control plane; **SAS** short-code pairing (six decimal digits from the `XX` handshake hash, confirmed out of band — defeats a LAN MITM, no camera); an Ed25519 **attestation** binding each device's identity key to its X25519 transport static; the signed **`peers.toml`** network trust ring (distinct from the unlock-ACL `trust.toml`); **mDNS** LAN discovery (`_softfig._tcp`); and a **zero-trust relay** (a blind ciphertext dumb-pipe through the always-on server for off-LAN peers, ring-authorized — no open relay). The transport key is a separate X25519 key minted at vault init beside the Ed25519 identity (auto-generated on unlock for existing vaults). **M5a-4** makes it operable: `softfig-keeperd` hosts the `softfig-net` instance on unlock (inbound listener + mDNS responder + optional relay listener), four new IPC verbs `pair_begin`/`pair_confirm`/`pair_list`/`pair_remove`, and the `softfig pair`/`peers`/`unpair` CLI. WireGuard was **rejected** — keeperd is an unprivileged user unit and WG's `CAP_NET_ADMIN` would re-break the FUSE/no-privilege model. **No new VCS intents** (M5a writes only vault-internal keys + `peers.toml`). The live two-device handshake + mDNS + relay forward is the documented manual real-machine smoke step (no multicast / second host / TTY in the sandbox). M5c (multi-ref union-mount), M5d (shared-subtree keying), M5e (write-turn sync) follow — see the garden's `meta/spec-sync.md` + `journal/decisions/decision-softfig-m5a-impl.md`.

**M5b (zero-knowledge device-chain replication) — DONE (2026-06-14)** is the **first slice where data crosses devices**. The chain owner pushes its signed, content-addressed device-chain **ciphertext** to explicitly-authorized backup hosts that **verify and store it but cannot decrypt it** (off-device backup + point-in-time DR via the VCS history). Reading peer documents stays out — deferred to a later **M5b-view** slice (a chain-scoped read subkey, not master key M). New **data-plane protobuf** in `softfig-net` (`control.proto` field nums 100+: `ReplicaGrant` / `GetTip` / `TipAnnounce` / `GetCommit` / `CommitData` / `GetTree` / `TreeData` / `GetObject` / `ObjectData` / `ReplicaDone`) + a frontend-neutral `replica` module: the `ReplicaSource` / `ReplicaSink` traits, the `serve_replication` (owner) + `pull_replication` (host, sink-driven) drive loops, **fast-forward-only** fork/rollback detection (a single honest owner never forks → a non-fast-forward is rejected + alarmed, never force-updated), and the domain-separated tip-announce / grant signing-byte helpers + verifiers. keeperd hosts it: `RepoSource` over a read-only `Repo` handle + a vault-signed announce, a keyless per-peer `MirrorStore` (reuses `softfig-store` `Db` + `ObjectStore` at `<replica_root>/<owner-id>/.softfig/`, verifies per-commit signature + hash + owner-binding, tree canonical addresses, object content addresses), a `GrantLedger` (`replica.toml` `push_to`), and the inbound-serve dispatch + owner push driver + a periodic reconcile loop (which doubles as the tip-driven pull-on-connect catch-up). **Two-sided consent:** `keeper.toml [replica] host` (static host opt-in, same posture as `[relay]`) + an owner-minted **signed grant** from `push_to` — pairing alone authorizes nothing. New IPC verbs `replica_grant` / `replica_revoke` / `replica_status` + `softfig replica grant|revoke|status`. **No new VCS intents** (the mirror is a verified copy, not an authored chain). Live two-device replication over real mDNS/relay is the manual real-machine smoke step; the loopback-TCP serve/pull/verify/fast-forward path is covered headlessly (`tests/m5b_replica.rs`). See the garden's `journal/decisions/decision-softfig-m5b-impl.md` "Update 2026-06-14 — landed".

**growlight (autonomous work-loop pillar) — Phase 2a DONE (2026-06-14)** is a new garden pillar + softfig feature driving an iterative *fresh-session work loop*: each session is reseeded from a curated **baton** (a pointer index, not a context dump), works one chunk against a **backlog** of milestones (→ ordered slices) and standalone tasks, hands off by rewriting the baton, and a fresh session continues — the bet being that a curated baton beats a lossy `/compact` (never compact; always `/clear`-reseed). Built device/agent-generalized for AUR distribution (no hardcoded paths; Claude Code is a documented agent backend behind a seam). **Phase 1** shipped the four daemon-mediated MCP verbs that maintain the pillar (`log_baton` / `add_backlog_item` / `add_slice` / `set_item_status`), reusing the small-files machinery: a shared `actions/numbering.rs` (`.seq`), the managed-region **authoritative queue table** in `growlight/backlog/CLAUDE.md` that owns each item's status + order (≤1 `active`, idempotent re-set), and lowercase-slug ids; `growlight/baton-log/` is excluded from the backlink graph (audit-only). **Phase 2a** adds **`softfig growlight init`** — an idempotent, daemon-mediated retrofit (mirrors `migrate split`) that materializes the `growlight/` pillar via that same verb machinery: the routing `CLAUDE.md`, the embedded `protocol.md` / `session-policy.md` templates (`templates/growlight/`, `include_str!`), and the backlog + baton-log skeleton, all folded into one `growlight_initialized` commit (no commit when nothing changed; self-healing for a partial pillar), plus best-effort + idempotent **§14 nav wiring** of the root `CLAUDE.md` (top-level-map bullet + boundary-table row, via a surgical "insert after the last list/table line" helper) and `meta/{reserved-filenames, conventions}.md`. The two budgets the loop honors (context 50/60 % ROLL; 5h-session 85 % HALT) read from a statusline-emitted `usage.json`. **Phase 2b** adds **`softfig growlight start`** — the launcher + the churny runtime under `$XDG_CONFIG_HOME/softfig/growlight/` (derived, never literal): it resolves the garden root from a read-only daemon STATUS query, requires the pillar present, then writes (plain fs ops, no IPC/commit) the generated `loop.json` (a SessionStart hook that re-injects `inject.sh` on both `startup` and `clear` — the `/clear` roll mechanism — plus a `statusLine` → `statusline.sh`), the two 0755 helper scripts (`inject.sh` cats the garden `protocol.md` + the runtime `baton.md` to stdout; `statusline.sh` tees `{context_window, rate_limits, ts}` to `usage.json` and prints a compact budget line), and seeds `baton.md` + `questions.md` once (never clobbering an in-flight loop). It then launches `claude --name softfig-loop --settings <loop.json>` (the documented agent-backend seam) or stops at `--no-launch`; warns when `jq` (the budget read path) is absent. The real-garden dogfood (install bins + cycle the daemon, then run `init`/`start`) is next. See the garden's `meta/spec-growlight.md` + `journal/decisions/decision-growlight-loop.md`.

**config-in-garden — Slice 1 + Slice 2 DONE (2026-06-15)** moves the daemon's per-garden config *inside* the garden so it is encrypted-at-rest, VCS-versioned, M5b-backed, and editable through the FUSE mount — instead of plaintext beside the vault under `<state_root>/.softfig/`. The framing that unlocked the simple design: each device's garden is its **own source of truth** (M5b is a one-way ciphertext mirror, never a merge), so one in-garden config doesn't bleed settings across devices. **Slice 1** lifts the post-unlock policy into `config/keeper.toml` (`GardenConfig` = `[net]`/`[relay]`/`[replica]`/`[reveal]`); the daemon overlay-reads it at unlock (after FUSE mount, before net start) via `KeeperConfig::apply_garden_config`, **falling back to the pointer when absent** (non-breaking). The plaintext `.softfig/keeper.toml` pointer shrinks to bootstrap-only — `state_root` (read *before* unlock to find the vault) + `[growlight] allow_relock` (kept OFF the agent-writable garden surface so the loop can't self-enable relock). **Slice 2** splits the trust ring at its root: slow-changing **membership** (device id / name / transport key / attestation / `paired_at`) → in-garden `config/peers.toml` (committed on pair/unpair only); volatile **endpoints** (host:port mDNS refreshes constantly) → a local `.softfig/peers-endpoints.toml` sidecar, **never committed** — so a discovery sighting can't fire a commit on every mDNS tick. `softfig-net` gains `EndpointCache` (load/save/`capture`/`apply`) + `Ring::to_membership_toml` (endpoints stripped); keeperd's `load_ring(garden_root, state_dir)` reads membership from the garden (legacy `.softfig/peers.toml` fallback) + merges the sidecar, the pairing handlers write membership through one self-write-suppressed write + a `peers_changed` commit, and the mDNS browse loop writes only the sidecar. `softfig migrate config [--apply]` (daemon-mediated, dry-run default, idempotent) lifts `config/keeper.toml` **and** the legacy ring's membership into `config/peers.toml` in one `config_migrated` commit (legacy ring absent ⇒ peers no-op; endpoints re-seeded into the sidecar). New VCS intents `config_migrated` (Slice 1) + `peers_changed` (Slice 2); onboard scaffolds `config/keeper.toml` but **not** an empty `config/peers.toml` (first pairing creates it). See `journal/decisions/decision-softfig-config-in-garden.md`.

| Area | State |
|---|---|
| Cargo workspace | ✅ twelve member crates (M5a: `softfig-net`) |
| `softfig-vault` crate | ✅ init / unlock / encrypt-blob / decrypt-blob / sign / rotate-key / recover; `at_state_root` for the M2a relocated layout; **M2b**: `layer_b` module with `derive_subkey` / `encrypt` / `decrypt`, `VaultSession::encrypt_layer_b` / `decrypt_layer_b` / `verify_master_passphrase`; **M2c**: `derive_region_subkey(M, path, id)` (`HKDF(M, salt=path‖0x00‖id, info=b"softfig:layer-b-region/v1")`) + `VaultSession::{encrypt_layer_b_region, decrypt_layer_b_region}` |
| `softfig-store` crate | ✅ ciphertext object directory (`.softfig/objects/<aa>/<rest>`) + sqlite metadata schema; WAL mode set on every open; `StorePaths` accepts a `state_root` distinct from `garden_root` |
| `softfig-vcs` crate | ✅ garden walker, tree blueprint, JCS-canonicalized + signed commits, log iteration, fsck; `Repo::open_with` accepts a state_root; `Repo::set_tip_changed_callback` for the FUSE driver to subscribe; **M2b**: `BlobEncryptor` trait + `Repo::set_blob_encryptor` + `tree::build_with` extension point so the daemon can route sealed paths through Layer B without leaking glob logic into `softfig-vcs`; **M2c**: `"manual_edit"` added to `KNOWN_INTENTS` (latent bug fix — classifier produced it but the closed-enum validator silently rejected it); **Slice 1 (small-files)**: `"note_added"` / `"note_revised"` added to `KNOWN_INTENTS`; **growlight**: `"baton_logged"` / `"backlog_item_added"` / `"slice_added"` / `"item_status_set"` (Phase 1) + `"growlight_initialized"` (Phase 2a) added to `KNOWN_INTENTS`; **config-in-garden**: `"config_migrated"` (Slice 1) + `"peers_changed"` (Slice 2) added to `KNOWN_INTENTS`; **task 020**: `"code_review_added"` + **020 slice 007**: `"code_review_revised"` added to `KNOWN_INTENTS` |
| `softfig-ipc` crate | ✅ JSON-Lines protocol envelope + typed verb args/replies + sync `connect`/`call` over `UnixStream`; includes `migrate_finalize`; **M2b**: `vault_reveal` / `vault_seal` / `vault_unseal` / `vault_list_sealed` verbs + corresponding args/replies + `MasterPasswordRequired` / `SealedPathNotFound` / `IdleStatusOnly` error kinds; **M2c**: `VaultRevealArgs.id: Option<String>` (skip-serializing) + `MalformedVaultTag` / `DuplicateVaultId` error kinds; **M3a**: `op::{log_decision, log_incident, archive, add_project, refresh_snapshot}` + five `*Args`/`*Reply` struct pairs + `InvalidSlug` / `InvalidProjectName` / `PathAlreadyExists` / `SourceNotFound` / `InvalidSnapshotPath` error kinds; **M3b**: `op::{list_tree, read_file}` read-only verbs + `ListTreeArgs`/`ListTreeReply` (`Vec<TreeEntry>`) + `ReadFileArgs`/`ReadFileReply` (`{path, content, sealed}`); **M5a-4**: `op::{pair_begin, pair_confirm, pair_list, pair_remove}` + `Pair*Args`/`Pair*Reply` (`PairPeer` / `PendingPairing`) + `PairFailed` error kind; **Slice 1 (small-files)**: `op::{add_note, revise_note}` + `AddNoteArgs`/`AddNoteReply` + `ReviseNoteArgs`/`ReviseNoteReply` + `NotAccretiveDir` error kind; **M5b**: `op::{replica_grant, replica_revoke, replica_status}` + `Replica*Args`/`Replica*Reply` + `HostedChain` (per-peer backup-health metadata); **growlight**: `op::{log_baton, add_backlog_item, add_slice, set_item_status}` + their `*Args`/`*Reply` pairs (Phase 1) + `op::growlight_init` + `GrowlightInit{Args,Reply}` (Phase 2a); **config-in-garden**: `op::migrate_config` + `MigrateConfig{Args,Reply}` (Slice 2 reply = `migrated`/`skipped` path lists, carrying the partial keeper.toml-vs-peers.toml state); **task 020**: `op::add_code_review` + `AddCodeReviewArgs`/`AddCodeReviewReply`; **M4 deploy (TUI slice)**: `op::{deploy_plan, deploy_apply}` + `DeployPlan{Args,Reply}` / `DeployApply{Args,Reply}` + `DeployAction` enum + `DeployPlanEntry` |
| `softfig-net` crate | ✅ **M5a** — frontend-neutral transport + control plane for cross-device sync. `transport` (Noise `XX`/`IK` over a `Read+Write` stream, stateless cipher state + `split()` for the relay's full-duplex forward), `proto` (prost `control.proto`: `Frame` oneof + `HelloPayload` + `Ping`/`Pong` + relay frames), `sas` (six-digit SAS from the `XX` handshake hash), `attest` (Ed25519 transport attestation), `pairing` (`pair_initiator`/`pair_responder` → `PendingPair`), `ring` (`peers.toml` load/verify/save), `discovery` (mDNS `_softfig._tcp` announce/browse + `refresh_ring_endpoints`), `relay` (blind ring-authorized dumb-pipe), `connect` (LAN-direct-first route policy); **M5b**: `replica` (data-plane drive — `ReplicaSource`/`ReplicaSink` traits, `serve_replication`/`pull_replication`, fast-forward/fork detection, `tipannounce_signing_bytes`/`grant_signing_bytes` + `verify_tipannounce`/`verify_grant`) + the data-plane `control.proto` messages (field nums 100+) + `NetError::Replica`; **config-in-garden Slice 2**: `ring::to_membership_toml` (serialize membership with endpoints stripped) + `endpoint_cache` module (`EndpointCache` load/save/`capture`/`apply` over the volatile `.softfig/peers-endpoints.toml` sidecar) — the membership/endpoint split that keeps mDNS churn out of the committed garden. Sync + threads (not tokio). Deps: `snow`, `prost`, `mdns-sd`, `hkdf`/`sha2`/`ed25519-dalek`/`hex`. Needs `protoc` at build time |
| `softfig-keeperd` binary | ✅ daemon with `SO_PEERCRED` + 0600 socket, full IPC verb set, mixed-mode dispatch on keeper.toml's `state_root` (**M-onboard**: `main.rs` now boots via `KeeperConfig::discover` so a born-in-FUSE/migrated garden auto-enters FUSE mode; absent keeper.toml falls back to M1c-compat), shared `DirtySetAccumulator` consumed by both `InotifyDriver` and FUSE via `AccumulatorSink`; **M2b**: `layer_b` module with `SealedPaths` matcher (globset-based) + `LayerBHook` doubling as `BlobEncryptor` and `SealedQuery`; four new IPC handlers; `DaemonInner` carries the shared hook + `last_reveal_at` idle-window state; `keeper.toml` gains `[reveal] idle_seconds = 0`; **M2c**: `layer_b` restructured as directory module — `layer_b/regions.rs` (RegionParser registry, scan + base64-disambiguator, write-path placeholder preservation, `with_substitutions` helper) + `layer_b/mod.rs` extensions (`LayerBHook` gains session + prior-tip slots, `BlobEncryptor::encrypt` got region-aware write path, `SealedQuery::redact_regions` impl, `PriorTipGuard` RAII helper around every commit_workdir site, `promote_manual_edit_for_new_ids` watcher sub-rule); `vault_reveal` handler gains the `id` branch (region-only reveal + temp-file naming + audit-intent extension); **M3a**: new `actions/` module (`conventions.rs` with hardcoded path/header/stub templates + slug/name/date validators + a civil-from-days date helper, plus one file per action) servicing the five typed write verbs — each validates, reject-on-exists, registers self-writes, then one `commit_workdir` under a `PriorTipGuard` via `actions::commit_now`; `server::dispatch` routes the five verbs straight to `actions::*`; `require_unlocked` / `validate_repo_path` / `path_to_repo_rel_string` promoted to `pub(crate)`; **M3b**: new read-only `reads` module (`list_tree` / `read_file` dispatch arms) — walks the tip tree via `Repo`, applies the installed `LayerBHook` (`is_sealed` → `[sealed:<path>]`, `redact_regions` → `[encrypted]`); reuses the M3a `pub(crate)` helpers, require-unlocked, no commits; **M5a-4**: new `net` module hosts the `softfig-net` instance — `NetRuntime::start` on unlock (inbound Noise listener + mDNS announce/browse + optional blind relay, all best-effort), `build_local_device` from the vault session, `DaemonInner` gains `net: Option<NetRuntime>` + `pending_pairs: PendingPairs` (parked `PendingPair`s awaiting SAS confirm, dropped on lock); `keeper.toml` gains `[net]` (`enabled`/`listen`/`device_name`) + `[relay]` (`enabled`/`listen`/`endpoint`/`static_key`); four pairing handlers run the blocking handshake with the daemon mutex **released**; **Slice 1 (small-files)**: `actions/add_note.rs` (`add_note` / `revise_note`) — accretive notes as numbered `NNN-slug.md` single-docs in `notes/`/`troubleshooting/` folders, per-folder `.seq` high-water mark (`next = max(.seq, highest live file) + 1`, never reused so archiving the newest leaves a gap), daemon-stamped `# title` + reviewed header, title/slug/number immutable; `add_project` now seeds `notes/.seq` instead of a `notes.md` monolith; `conventions.rs` gains `is_accretive_dir` / `note_filename` / `note_doc` / `slugify`; `migrate.rs` gains a pure, unit-tested `split_monolith` / `plan_split` splitter (monolithic notes.md → numbered notes; production trigger deferred to the migrate/onboard flow); **M5b**: new `replica` module (`RepoSource` = `ReplicaSource` over a read-only `Repo` + signed announce; `MirrorStore` = keyless `ReplicaSink` over a per-peer `softfig-store` mirror; `GrantLedger` for `replica.toml` `push_to`; `mint_grant`; `list_hosted` status reads) + `net.rs` host-serve dispatch (first frame `Ping` vs `ReplicaGrant` → `pull_replication` into the mirror) + owner push driver + reconcile loop; `handlers.rs` gains `replica_grant`/`replica_revoke`/`replica_status`; `config.rs` gains `ReplicaConfig` + `replica_root`; `keeper.toml` gains `[replica] host`; **growlight**: `actions/growlight/` — Phase 1 verbs (`log_baton`/`add_backlog_item`/`add_slice`/`set_item_status`) over the shared `actions/numbering.rs` (`.seq`) + the `managed` queue table + a `paths.rs` path/template vocab + a `queue.rs` table parser, and Phase 2a `init.rs` (`growlight_init` — idempotent pillar scaffolder from `templates/growlight/` via the same machinery + best-effort §14 nav wiring + a pure unit-tested nav helper, one `growlight_initialized` commit or none); **config-in-garden**: `keeper_toml::GardenConfig` (`config/keeper.toml`) + `KeeperConfig::apply_garden_config` overlay-read at unlock with pointer fallback (Slice 1); `actions/migrate_config.rs` (`migrate_config` — daemon-mediated, dry-run default, idempotent, lifts keeper.toml + legacy-ring membership into `config/{keeper,peers}.toml` in one `config_migrated` commit); `net.rs` `load_ring(garden_root, state_dir)` (membership from `config/peers.toml`, legacy fallback, sidecar merge) + `write_and_commit_membership` (one self-write + `peers_changed` commit + sidecar refresh) + browse loop writes the sidecar not the ring; `persist_ring_entry` removed (Slice 2); **task 020**: `actions/add_code_review.rs` (`add_code_review` — numbered `code-reviews/` records sharing `add_note`'s write core, commits `code_review_added`); **M4 deploy (TUI slice)**: `deploy.rs` (`deploy_plan`/`deploy_apply` handlers over the `softfig-deploy` crate; `server::dispatch` routes `DEPLOY_PLAN`/`DEPLOY_APPLY`) |
| `softfig-tui` binary | ✅ **M3b** — ratatui frontend (lib + bin): master/detail Browse (lazy `list_tree` tree + `read_file` preview), History (`log`/`show`), modal Action forms over the six M3a write verbs (built-in multi-line text area, no `$EDITOR`), in-app unlock, `:` command palette; worker-thread IPC client (`ipc.rs`), pure-state modules (`tree`, `forms`, `command`, `textarea`, `app`) carry the unit tests + a `TestBackend` render snapshot. **M3b vault follow-up (2026-06-01):** a `3:Vault` master/detail tab over `vault_list_sealed`, `seal`/`unseal` as two more `ActionKind` forms, and a masked whole-file **reveal** flow that shows only the daemon's `{temp_path, expires_at}` (never plaintext) + a `c` copy that pipes the 0600 temp file straight into `wl-copy` (stdin = the file, never the TUI heap); new `clip.rs`. **M5a pairing follow-up (2026-06-10):** a `4:Peers` master/detail tab over the four `pair_*` verbs — ring members + pending pairings in one flattened selection list, `p` initiate (`pair_begin` → SAS-confirm overlay), `Enter` confirm a parked pending (responder side), `D` unpair, `r`/auto refresh via `pair_list`; `:peers`/`:pair`/`:unpair` palette commands; pure frontend wiring (no new IPC/crypto). **tui-modernize (on `feat/tui-modernize`, unmerged; slices land in order — milestone `deferred` pending the human's live-render smokes + stack merge):** `5:Backup` tab over M5b `replica_status`/`grant`/`revoke` (slice 001, merged to `main`); `6:Deploy` tab over M4 plan/apply (slice 002 — the one sanctioned daemon addition: thin `deploy_plan`/`deploy_apply` IPC verbs); inline-region **`--id` vault reveal** in the Vault flow (slice 003 — `Overlay::RevealRegion` region picker off the Browse preview, rides `vault_reveal`'s `id`); a **read-only `7:Growlight` section** (slice 004) gated on the daemon-owned `StatusReply.growlight_enabled` bit (020 slice 001 — keeperd's fail-closed `fleet_enabled()` read, refreshed every status tick; the client never probes `config/growlight.toml` presence, which disagrees on a fresh garden) — the backlog queue rendered from the structured `growlight_queue` verb's daemon-parsed rows (020 slice 002; no client `split('|')`), the latest baton over `read_file`; the view snaps back to Browse if the gate flips off mid-session (020 slice 013) |
| `softfig-mcp` binary | ✅ stateless stdio bridge — `initialize` / `tools/list` / `tools/call`; **M3a**: `log_decision` / `log_incident` / `archive` / `add_project` / `refresh_snapshot` + the `replace_file` verbatim escape hatch (Slice 3 renamed it from `propose_doc_update`); **small-files**: + `add_note` / `revise_note` (Slice 1) + `add_section` / `edit_section` / `append_to_section` / `set_reviewed` (Slice 2); **growlight Phase 1**: + `log_baton` / `add_backlog_item` / `add_slice` / `set_item_status` → **16 tools**; **task 020 (code-review records)**: + `add_code_review` (numbered review records in `code-reviews/` folders; body follows the review template) → **24 tools** with the later growlight/coordination verbs. `tool_defs()` returns the schemas; pure `resolve_tool(name, args)` maps a tool name → IPC op + validated args (unit-tested); `summarize()` renders the one-line reply. (`growlight init` is a CLI/IPC verb, not an MCP tool.) |
| `softfig-cli` (`softfig` binary) | ✅ vault subcommands + VCS subcommands + `daemon {start, stop, status, unlock}` + bridge fast path on commit/log/show/fsck + `migrate {prepare, finalize}` + no-arg `migrate` phase status; migrated gardens refuse direct-mode `commit/log/show/fsck`; **M2b**: `softfig reveal <path>` and `softfig vault {seal, unseal, list-sealed}`; **M2c**: repeatable `--id <id>` flag on `reveal` with single-prompt master-password caching across multiple region calls; **M-onboard**: `softfig onboard [--garden-root P] [--state-root P] [--machine NAME] [--customize] [--yes]` first-run wizard (`cmd_onboard.rs`), thin TTY frontend over `softfig-onboard`; stops before the passphrase step when no TTY; **M4a**: `softfig deploy`; **M5a-4**: `softfig pair <fingerprint> [--endpoint] [--yes]` / `softfig peers` / `softfig unpair <id>` (`cmd_pair.rs`) — daemon-only, SAS-confirm prompt; relay hosting is a `keeper.toml [relay]` flag, not a subcommand; **M5b**: `softfig replica grant <fp>` / `revoke <fp>` / `status` (`cmd_replica.rs`) — daemon-only; host opt-in stays a `keeper.toml [replica]` flag; **growlight Phase 2a/2b**: `softfig growlight init` (daemon-only idempotent pillar scaffolder) + `softfig growlight start` (`cmd_growlight.rs`) — sets up the `$XDG_CONFIG_HOME/softfig/growlight/` runtime (generated `loop.json` + `inject.sh`/`statusline.sh` + seeded `baton.md`/`questions.md`, plain fs, no IPC/commit) and launches `claude --name softfig-loop --settings <loop.json>` (`--no-launch` to just generate); **semi-auto-backend-seam** (merged 2026-08-12): that launch is now a real `Backend` seam in `growlight_backend.rs` — `claude` (same argv, now rooted at the garden) | `opencode` (a generated `opencode.json` handed over via `OPENCODE_CONFIG`: a `softfig-loop` primary agent whose prompt pulls the garden `protocol.md` in by `{file:…}` reference + a step-0 baton-boot line, rooted at the garden so its docs are ordinary in-project reads, with `external_directory` granting only the runtime root (the baton) + the claude-memory tree — slice 006 removed the old garden deny, which blocked the loop's only read channel and was unexpressible anyway, and an explicit project-scoped `mcp.softfig-mcp` block), selected by `--backend`/`--opencode`/`--model` or an interactive `1/2/3/4` picker, with the model resolvable from the active backlog item's own `> Model:` declaration; headless `--auto` stays claude-only; **config-in-garden**: `softfig migrate config [--apply]` (`cmd_migrate.rs`) — daemon-mediated, dry-run default, lifts keeper.toml + peers.toml membership into the in-garden `config/` (renders the `migrated`/`skipped` path lists) |
| `softfig-onboard` crate | ✅ **M-onboard** — frontend-agnostic scaffold core: embedded `templates/default-garden/` via `include_dir!`, `OnboardOptions` / `ScaffoldPlan`, `plan()` (placeholder substitution + concept-dir filter + defensive `.keep` synthesis), `apply()`, `onboard()` / `onboard_with_params()` (Vault init at `state_root` → stamp to tempdir → `Repo::create_fresh` → drop staging → write keeper.toml pointer); depends on `softfig-vault` + `softfig-vcs` |
| `softfig-deploy` crate | ✅ **M4a** — frontend-neutral deploy spine: `DeployConfig` (TOML `[dots]` `name = {source, target, method?}`), `DeployPaths`, `plan()` (read-only diff → `Action::{CreateSymlink, ReplaceManaged, CopyStamped, SkipUnchanged, Conflict}`), `apply()` (per-entry atomic temp+rename, idempotent, `Report`), symlink-to-cache (`0600`/`0700`) + copy-with-stamp fallback, `$HOME`-only + file-only target validation, `--force` backup to `<target>.softfig-bak`. Pure-std + `serde`/`toml`/`tempfile`; no Vault/daemon deps |
| `softfig-fuse` crate | ✅ M2a — `FuseMount::mount(garden, state, session, sink)` returns a `MountHandle`; reads decrypt blobs via the shared `VaultSession`, writes go through an in-memory overlay that the daemon's M1d accumulator flushes into commits; `MountHandle::on_tip_changed` clears the overlay + stat cache on each new tip; **M2b**: `FuseMount::mount_with(..., Option<Arc<dyn SealedQuery>>)` projects `[sealed:<path>]\n` placeholders for sealed reads (computed on read, never stored); **M2c**: `SealedQuery::redact_regions` default-impl method + `SharedState::redacted_cache` (broadcast invalidation on `tip_changed`); read path funnels post-Layer-A bytes through `redact_regions` for non-whole-file-sealed paths |
| Layer B selective secrets | ✅ M2b file-level seal/unseal + `softfig reveal` flow + auto-migration on glob add; ✅ M2c inline `<vault id="…">…</vault>` regions + per-region reveal + classifier auto-promotion on new-id introduction |
| Onboarding / first-run | ✅ **M-onboard** — `softfig onboard` scaffolds a fresh garden born-in-FUSE; install runbook + `scripts/onboard-device.sh`; program repo under local git |
| MCP action surface | ✅ **M3a** — six daemon-mediated MCP tools; the daemon stamps garden conventions (path/header/intent/payload) from hardcoded Rust defaults, so any Claude session writes the right file without learning the conventions |
| TUI (ratatui) | ✅ **M3b** — `softfig-tui` over the daemon (Browse/History/Actions/unlock) + read-only `list_tree`/`read_file` verbs with daemon-side redaction. **Vault surface shipped 2026-06-01** (`3:Vault` tab: list-sealed + seal/unseal forms + masked whole-file reveal with clipboard copy, plaintext never shown in the TUI). **Inline-region `--id` reveal shipped** (tui-modernize slice 003). **tui-modernize catch-up (branch `feat/tui-modernize`, unmerged; slices 2–4 code-complete, milestone `deferred` pending live-render smokes):** `5:Backup` + `6:Deploy` + growlight-gated read-only `7:Growlight` tabs. Trust-matrix, TPM still deferred |
| Templating + symlinks pillar (M4) | 🔨 **M4a deploy spine ✅ (2026-06-02)**; M4b templating (MiniJinja), M4c secrets+posthooks, `/etc` deploy still to come |
| Cross-device sync pillar (M5) | 🔨 **M5a transport + pairing ✅ COMPLETE (2026-06-09 → 2026-06-10)** + **M5b zero-knowledge replica ✅ (2026-06-14)** — the first data-crossing slice: signed content-addressed device-chain ciphertext pushed to authorized backup hosts that verify + store but can't decrypt (two-sided consent; fast-forward-only verified mirror; `softfig replica` surface). Reading peer docs → **M5b-view**. M5c (multi-ref union-mount), M5d (shared-subtree keying), M5e (write-turn sync) follow |
| growlight (autonomous work-loop pillar) | 🔨 **Phase 1 backlog/baton MCP verbs ✅ + Phase 2a `softfig growlight init` ✅ + Phase 2b `softfig growlight start` ✅ (2026-06-14)** — daemon-mediated backlog/baton verbs over the small-files machinery + the idempotent pillar scaffolder (embedded templates + §14 nav wiring) + the runtime/launcher (`$XDG_CONFIG_HOME/softfig/growlight/` `loop.json`/`inject.sh`/`statusline.sh`/`baton.md`/`questions.md`, plain fs no IPC, then launch the agent). The real-garden dogfood shipped; the full-auto orchestrator shipped as **growlightd** — the one control plane for autonomous sessions, a single-agent run being a fleet-of-one (the standalone auto-loop CLI driver was retired in its favour, on branch awaiting human merge). Onboard opt-in is the remaining roadmap item. See `meta/spec-growlight.md` + `journal/decisions/decision-growlight-loop.md` |
| GUI, AUR | not started — M6+ |

Spec source-of-truth still lives in the garden at `~/soft-fig_garden/meta/spec-*.md`. Read those for design intent.

## Workspace layout

```
software-config_garden/
├── Cargo.toml                          # [workspace]
├── .gitignore                          # excludes target/, keeps Cargo.lock
├── templates/default-garden/           # M-onboard — embedded skeleton (include_dir!)
├── scripts/onboard-device.sh           # M-onboard — install runbook script
├── docs/onboard-laptop.md              # M-onboard — manual onboarding runbook
├── packaging/systemd/
│   └── softfig-keeperd.service         # drafted user unit (mirror to ~/dotfiles/...)
└── crates/
    ├── softfig-vault/                  # crypto + key lifecycle
    │   └── src/{lib,vault,session,kek,master,identity,blob,recovery,storage,params,error}.rs
    ├── softfig-store/                  # objects + sqlite metadata
    │   └── src/{lib,db,objects,paths,hash,error}.rs
    ├── softfig-vcs/                   # VCS operations
    │   └── src/{lib,repo,walk,tree,commit,intent,log,fsck,error}.rs
    ├── softfig-ipc/                    # JSON-Lines protocol + client
    │   └── src/{lib,proto,verbs,socket,client}.rs
    ├── softfig-keeperd/                # daemon binary
    │   ├── src/{lib,main,config,daemon,server,handlers,reads,peer,classify,watcher,state,
    │   │        keeper_toml,migrate,fuse_sink,net,deploy}.rs   # M5a-4: net hosts softfig-net; deploy = M4 plan/apply IPC verbs
    │   ├── src/layer_b/{mod,regions}.rs  # M2b matcher + hook; M2c regions + write-path
    │   └── src/actions/{mod,conventions,log_decision,log_incident,add_note,add_code_review,
    │            archive,add_project,refresh_snapshot}.rs  # M3a + Slice-1 + task-020 write verbs
    ├── softfig-mcp/                    # stdio MCP bridge
    │   └── src/main.rs
    ├── softfig-cli/                    # `softfig` binary
    │   └── src/{main,cmd_vault,cmd_repo,cmd_daemon,cmd_migrate,cmd_reveal,cmd_onboard,
    │            cmd_deploy,cmd_pair}.rs               # cmd_pair: M5a-4 pair/peers/unpair
    ├── softfig-fuse/                   # M2a — FUSE plaintext-view of Layer A
    │   └── src/{lib,fs,inodes,overlay,tree_view}.rs
    ├── softfig-onboard/                # M-onboard — scaffold core (frontend-agnostic)
    │   └── src/{lib,keeper_pointer,tests}.rs
    ├── softfig-tui/                    # M3b — ratatui frontend (lib + bin); +vault tab & clip.rs (2026-06-01)
    │   └── src/{lib,main,app,ui,ipc,tree,forms,command,textarea}.rs
    ├── softfig-deploy/                 # M4a — deploy spine (frontend-neutral plan/apply)
    │   └── src/{lib,config,stamp,plan,apply,tests}.rs
    └── softfig-net/                    # M5a — transport + control plane (frontend-neutral)
        ├── build.rs + proto/control.proto           # prost-generated control plane
        └── src/{lib,transport,proto,sas,attest,pairing,ring,discovery,relay,connect,error}.rs
                                                     # M5b: + replica.rs (data-plane drive)
    # softfig-keeperd also gains src/replica.rs (RepoSource + MirrorStore + GrantLedger) — M5b
    # softfig-cli also gains src/cmd_replica.rs (softfig replica grant/revoke/status) — M5b
    # softfig-keeperd gains src/actions/growlight/{mod,paths,queue,init}.rs — growlight Phase 1 verbs + Phase 2a init
    # softfig-cli gains src/cmd_growlight.rs (softfig growlight init + start); templates/growlight/{protocol,session-policy}.md — growlight Phase 2a/2b
    # growlight runtime is NOT in the repo — generated by `growlight start` under $XDG_CONFIG_HOME/softfig/growlight/ (loop.json/inject.sh/statusline.sh/baton.md/questions.md)
```

## On-disk layout

**M1c-compat (no `state_root` in keeper.toml):**

```
<garden_root>/.softfig/
├── vault/              # owned by softfig-vault: master keys, identity, KEK wrappings, params
├── objects/<aa>/<rest> # ciphertext blobs, addressed by BLAKE3(ciphertext)
├── db.sqlite           # commits / trees / tree_entries / refs / meta (schema_version = 1)
└── keeper.toml         # optional; absent or empty = M1c-compat
```

**M2a (FUSE-mounted):** the daemon mounts a FUSE filesystem at
`<garden_root>` (the mount hides whatever plaintext is there during
the prepare phase, then `migrate finalize` deletes the orphan).
On-disk state moves to `<state_root>/.softfig/` (default
`~/.local/share/softfig/<repo_id>/.softfig/`):

```
<state_root>/.softfig/   # canonical state in M2a
├── vault/
├── objects/<aa>/<rest>
├── db.sqlite
└── keeper.toml          # state_root = "<state_root>"
```

**Born-in-FUSE (M-onboard):** `softfig onboard` creates this same M2a
layout directly (no `migrate`). The encrypted state is written to
`<state_root>/.softfig/` from the first commit; a `keeper.toml` pointer is
written to **both** `<garden_root>/.softfig/keeper.toml` (where
`KeeperConfig::discover` reads it) and `<state_root>/.softfig/keeper.toml`.
No plaintext is ever written at `<garden_root>` — the daemon serves it via
FUSE on unlock.

## Crypto + canonicalization

- AEAD: XChaCha20-Poly1305 (Vault blob layer)
- Hash: BLAKE3 (object addresses, tree hashes, commit hashes)
- Password KDF: Argon2id (OWASP 2023 default `m=64 MiB, t=3, p=4`)
- Subkey KDF: HKDF-SHA-256
- Signing: Ed25519 (identity key, signs each commit's hash)
- Recovery phrase: BIP39 12-word
- **Canonicalization for hashing/signing: RFC 8785 JCS** via `serde_jcs`. Trees, commits, and intent payloads all canonicalize through it.

Blobs use **master-keyed convergent encryption** so the VCS layer can content-address ciphertext and dedup: `nonce = BLAKE3-keyed(M, plaintext)[..24]`, `per_blob_key = HKDF-SHA-256(salt=nonce, ikm=M, info="softfig.blob.v1")`, `blob_file = varint(master_key_id) ‖ nonce ‖ AEAD-body`. Same plaintext + same M → same ciphertext → same hash. Indistinguishable without M.

Commits are signed by hashing the canonical commit form first: `commit_hash = BLAKE3(JCS({parent, root_tree, author_device, author_pubkey, intent, payload, master_key_id, timestamp}))`, then `signature = identity.sign(commit_hash)`. Verifiers re-canonicalize, re-hash, and check both the hash matches the stored row and the Ed25519 signature verifies under `author_pubkey`.

## Build / test / run

```bash
cd ~/projects/software-config_garden
cargo build --workspace          # debug build (needs `protoc` for softfig-net's prost build)
cargo test --workspace           # 418 tests (growlight Phase 1 verbs + Phase 2a init + Phase 2b start runtime/launcher generators: 7 new CLI unit tests; M5b replica; pairing-UX Slice A; M5a-1..4; M4a; M3a/M3b/M-onboard/M2x carryovers), <3s with fast Argon2
cargo clippy --workspace --all-targets -- -D warnings
cargo run -p softfig-cli -- --help
cargo run -p softfig-cli -- vault --help
cargo run -p softfig-cli -- init --help
```

Tests use minimum Argon2id cost (`m_cost=8 KiB, t_cost=1, p_cost=1`) so the suite runs in <1 second; production defaults are OWASP 2023.

## CLI surface

```text
softfig onboard [--garden-root P] [--state-root P] [--machine NAME] [--customize] [--yes]  # M-onboard first-run wizard
softfig vault init        | status | rotate-key | recover         # crypto state
softfig init              # writes a genesis commit over the existing vault
softfig commit --intent <name> -m <msg> [-f <path>]... [--kv k=v]... [--payload-json '{...}']
softfig log [--limit N]
softfig show [<commit-hex>]
softfig fsck
softfig daemon start | stop | status | unlock                     # M1c daemon controls
softfig migrate                                                    # M2a — phase status (no daemon needed)
softfig migrate prepare [--state-root <path>]                      # M2a phase 1 (no daemon, refuses if socket present)
softfig migrate finalize                                           # M2a phase 3 (IPC verb to running M2a daemon)
softfig deploy [--garden-root P] [--cache-root P] [--dry-run] [--force]  # M4a — materialize config/deploy.toml onto the FS
softfig pair [<fingerprint>] [--endpoint host:port] [--yes]       # M5a-4 — pair with a peer (SAS confirm); no-arg → LAN pick-list (Slice A)
softfig peers                                                     # M5a-4 — list the network trust ring + pending pairings
softfig unpair <fingerprint>                                     # M5a-4 — remove a peer from the ring
softfig replica grant <fingerprint>                              # M5b — authorize a paired host to back up this device's chain
softfig replica revoke <fingerprint>                             # M5b — stop pushing to a host (it keeps what it holds)
softfig replica status                                           # M5b — backup health: push_to, host flag, per-peer mirror stats
softfig growlight init                                           # growlight Phase 2a — scaffold the growlight/ pillar (idempotent; daemon-mediated)
softfig growlight start [--no-launch] [--garden-root P] [--config-dir P]  # growlight Phase 2b — set up the runtime + launch the loop agent
softfig growlight start [--backend claude|opencode] [--opencode] [--model <spec>]  # semi-auto-backend-seam — choose backend + model
    # <spec> = claude | reasoning | flash | auto | provider/model, with an optional ` variant=<v>` tail (alias ` effort=<v>`; opencode only).
    #   reasoning = deepseek/deepseek-v4-pro · flash = deepseek/deepseek-v4-flash (the default whenever opencode is chosen without a model).
    #   auto ("let growlight decide") = use the model the ACTIVE backlog item declares in its `> Model:` line — the active slice doc, else the
    #     milestone doc, else flash. A malformed declaration is reported and defaulted, never a launch failure.
    # No selector on a TTY brings up the 1/2/3/4 picker; the three selectors are mutually exclusive; non-TTY / --auto / --no-launch never prompt
    # and default to claude. The launch cwd is fixed per backend: claude roots at the garden (auto-cd), opencode at the runtime dir.
```

**Pairing-UX Slice A (LAN pick-list)** removes the "type a long fingerprint"
friction on the LAN — no new crypto, the SAS still authenticates, the pick-list
only *addresses*. The mDNS TXT gains an `nm` (name) field (`keeper.toml [net]
advertise_name = true`, default on, falls back to fingerprint-only when off); a
read-only `discover_list` IPC verb surfaces the daemon's mDNS discovery cache
(nearby devices not yet in the ring); `softfig pair` with **no argument** shows
an interactive by-name picker; the TUI `4:Peers` tab gains a "discovered nearby"
section where `p`/Enter on a device auto-fills its fingerprint + endpoint into
the existing `pair_begin` → SAS flow. See the garden's
`journal/decisions/decision-softfig-pairing-ux.md` "Slice A". (Slice B — PAKE
numeric-code rendezvous, off-LAN — stays deferred/paper-locked.)

`commit/log/show/fsck` auto-detect a running daemon by `connect()`-ing
to `$XDG_RUNTIME_DIR/softfig-keeperd.sock`; on success the operation
runs through the daemon (no per-command passphrase prompt). The CLI
falls back to direct mode **only** when the socket is absent
(`ENOENT`/`ECONNREFUSED`). A reachable-but-erroring daemon (e.g.,
`vault_locked`) is surfaced verbatim — never bypassed — so the
"daemon, when present, is the sole writer" invariant holds.

## IPC surface (softfig-ipc)

JSON-Lines on a Unix socket. Verbs: `status`, `unlock`, `commit`,
`log`, `show`, `fsck`, `propose_doc_update`, `migrate_finalize`,
`shutdown`; M2b: `vault_reveal` / `vault_seal` / `vault_unseal` /
`vault_list_sealed`; **M3a**: `log_decision` / `log_incident` /
`archive` / `add_project` / `refresh_snapshot`; **Slice 1
(small-files)**: `add_note` / `revise_note` (numbered notes in accretive
folders); **task 020**: `add_code_review` (numbered code-review records in
`code-reviews/` accretive folders, primary home
`projects/<project>/code-reviews/`; commit `code_review_added`; shares
`add_note`'s write core); **M3b**: `list_tree` /
`read_file` (read-only browse; daemon redacts sealed content — `ReadFileReply` also carries CAS `version`/`sections` and, per 020 slice 003, daemon-computed `region_ids` for the file's inline `<vault>` sealed regions, so frontends never re-parse the projection); **M4 deploy (TUI slice)**: `deploy_plan` (read-only plan/diff) / `deploy_apply` (`{force}`; materializes `config/deploy.toml` onto the FS, daemon-run); **020 hardening**: `StatusReply.growlight_enabled` (slice 001 — the daemon's fail-closed growlight gate on every `status` reply, read off-lock) + `growlight_queue` (slice 002 — read-only; the default backlog queue as daemon-parsed structured rows `{id, title, status}`, `\|` cell escape round-trips). Auth = filesystem mode 0600 + `SO_PEERCRED` UID-match
on every accept. Daemon boots in **Locked** state; only `status`
and `unlock` are answerable until `unlock` succeeds. All M3a/M3b +
small-files write verbs require Unlocked.
`migrate_finalize` requires `state_root` to be set in `keeper.toml`
AND the daemon to have an active FUSE mount. **M5a-4**: `pair_begin` /
`pair_confirm` / `pair_list` / `pair_remove` (cross-device pairing; require
Unlocked). `pair_begin` runs the blocking Noise `XX` handshake with the daemon
mutex released and parks the result; `pair_confirm` persists the peer into
`peers.toml`. The daemon hosts the `softfig-net` instance (inbound listener +
mDNS + optional `[relay]`) on unlock. **Pairing-UX Slice A**: `discover_list`
(read-only; require Unlocked) surfaces the mDNS discovery cache's
nearby-but-unpaired devices `{name, fingerprint, endpoint, last_seen_secs}` as
the LAN pick-list — filtered against the ring + this device, fed by the TXT
`nm` name field. **M5b**: `replica_grant` / `replica_revoke` / `replica_status`
(require Unlocked) — owner-side controls for zero-knowledge device-chain backup.
`replica_grant`/`revoke` edit the `replica.toml` `push_to` allow-list (grant
requires the fingerprint to name a paired ring member); `replica_status` returns
backup-health metadata only (`{host, push_to, hosted[]}`), never peer documents.
The replication data plane rides the existing Noise sessions (not the IPC socket):
the host serves the inbound listener's `ReplicaGrant`-first sessions into a
keyless per-peer mirror, and the owner's reconcile loop dials granted hosts.

## Cross-refs

- Garden view: `~/soft-fig_garden/projects/software-config_garden/` — milestone tracking, decisions, notes.
- Vision: `~/soft-fig_garden/meta/program-vision.md`.
- Design playgrounds: `~/soft-fig_garden/meta/spec-{vault,vcs,keeper,sync,templating,symlinks}.md`.
- Decision log: `~/soft-fig_garden/journal/decisions/decision-add-keeper-vision.md`, `decision-vault-pillar.md`, `decision-trust-matrix.md`, `decision-softfig-vault-impl.md`, `decision-softfig-vcs-impl.md`, `decision-softfig-keeperd-impl.md`, `decision-softfig-watcher-refactor-impl.md`, `decision-softfig-fuse-impl.md`, `decision-softfig-layer-b-impl.md`, `decision-softfig-m2c-impl.md`, `decision-softfig-onboard-impl.md`, `decision-softfig-m3a-impl.md`, `decision-softfig-m3b-impl.md`, `decision-softfig-m3b-vault-tui-impl.md`, `decision-softfig-m4-impl.md`, `decision-softfig-m5a-impl.md` (transport + pairing; see also `docs/m5a-impl-plan.md`), `decision-softfig-m5b-impl.md` (zero-knowledge replica; "Update 2026-06-14 — landed").

## How to behave here

- Code-level architecture, build commands, FFI contracts → expand this CLAUDE.md.
- Planning, milestones, decisions → garden's `projects/software-config_garden/`.
- Don't duplicate spec content from `meta/spec-*.md` here; point at it.
- **M5a (transport + pairing) shipped** (`softfig-net` + keeperd net host + `softfig pair`/`peers`/`unpair`) — the cross-device *channel + recognition* foundation. **M5b (zero-knowledge replica) shipped** (2026-06-14) on top of it — the first slice where data crosses devices: signed content-addressed device-chain ciphertext to authorized backup hosts that verify + store but can't decrypt; two-sided consent; fast-forward-only verified mirror; `softfig replica` surface. Still deferred, each warranting its own decision-file iteration: reading peer docs (M5b-view, chain-scoped read subkey), M5c multi-ref union-mount → M5d/M5e shared subtrees + write-turn, relayed off-LAN push, real-time push-on-commit signalling, the unlock trust-matrix / peer-unlock / panic-counter, TPM self-path, replica-only mode. **Pairing ≠ unlock authority ≠ shared-subtree membership** — the M5a `peers.toml` ring is the network layer only; don't conflate it with the unlock-ACL `trust.toml`. Layer B shipped in M2b (whole-file seals) and M2c (inline `<vault id="…">…</vault>` regions); the FUSE plaintext view, the `softfig reveal` flow, and the audit-intent shape are all live.
- Daemon/MCP shipped in M1c; the live `notify-debouncer-full` event loop landed same-day (2026-05-09). M1d (2026-05-10) restructured the watcher into a source-agnostic `DirtySetAccumulator` + `DirtySetSource` trait + `InotifyDriver`. M2a (2026-05-10) added the FUSE driver as the second source: writes through the FUSE mount fire `DirtyEvent`s into the SAME accumulator inotify uses (single classifier pipeline). 200 ms debounce, 500 ms suppress TTL, classifier rules (`decision_logged`, `incident_logged`, `archive_move`, `manual_edit`) all preserved bit-for-bit.
- M2a runs in **mixed mode**: a single `softfig-keeperd` binary handles both M1c-compat (no `state_root` in keeper.toml) and M2a (state_root present). Choosing between modes is data-driven — there is no flag — so the binary is daily-driveable against an unmigrated garden today. Migration is non-destructive and three-phase (`migrate prepare` → daemon start → `migrate finalize`); reversible at every phase until finalize.
- Schema migrations: add new variants to the `Intent` closed enum + bump `KNOWN_INTENTS` only after updating `meta/spec-vcs.md` and writing a `decision-*` file. The `init` variant added in M1b is an example.
- Sqlite schema lives in `softfig-store::db::SCHEMA_V1`. Future migrations go through a migration table; don't edit the v1 string in place.
